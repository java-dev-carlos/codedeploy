version: 0.2

phases:
  install:
    commands:
      - echo "Install phase: Install dependencies here, if any"
  build:
    commands:
      - echo "Build phase: Run build commands here"
      # Example: npm install, pip install, gradle build, etc.
  post_build:
    commands:
      - echo "Passing to deployment"